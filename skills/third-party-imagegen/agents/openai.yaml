interface:
  display_name: "Third-Party ImageGen"
  short_description: "Generate GPT images through a custom API"
  default_prompt: "Use $third-party-imagegen to generate an image through my configured token service."
